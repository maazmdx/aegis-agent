# Runtime + test dependencies for Aegis.
# Loosened from exact pins to minimum known-good versions so `pip install`
# resolves against currently-published releases (some previous exact pins did
# not exist on PyPI). Tighten these again before a production release if you
# need fully reproducible builds.
flask>=3.0
gunicorn>=21.2
google-adk>=1.0.0
google-cloud-firestore>=2.16.0
google-cloud-pubsub>=2.21.0
google-genai>=1.0.0
requests>=2.31.0
tenacity>=8.2.0
pytest>=8.0.0
